apiVersion: v1
kind: Service
metadata:
  name: {{ include "openfoundry.fullname" . }}-gateway
  labels:
    {{- include "openfoundry.labels" . | nindent 4 }}
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.gateway.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openfoundry.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: gateway
