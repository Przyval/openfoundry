import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import { resolve } from "node:path";

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    proxy: {
      "/api": {
        target: "http://localhost:8080",
        changeOrigin: true,
      },
      "/multipass": {
        target: "http://localhost:8080",
        changeOrigin: true,
      },
    },
  },
  build: {
    outDir: "dist",
    sourcemap: true,
    rollupOptions: {
      onwarn(warning, warn) {
        if (warning.code === "MODULE_LEVEL_DIRECTIVE") return;
        warn(warning);
      },
    },
  },
  resolve: {
    alias: {
      // Shim node:crypto for browser — auth-tokens imports it but
      // the console only uses sdk-oauth's browser-safe PKCE parts.
      "node:crypto": resolve(__dirname, "src/shims/crypto-shim.ts"),
    },
  },
});
